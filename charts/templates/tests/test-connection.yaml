apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reddvid.fullname" . }}-test-connection"
  labels:
    {{- include "reddvid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reddvid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
